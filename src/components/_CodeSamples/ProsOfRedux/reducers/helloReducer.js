const initialState = {
  text: "Hello World",
};

const helloReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default helloReducer;
